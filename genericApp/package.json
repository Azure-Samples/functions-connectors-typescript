{
  "name": "genericapp",
  "version": "1.0.0",
  "description": "Azure Functions sample using the generic connectorTrigger<TItem> API from @azure/functions-extensions-connectors — works for any Azure Logic Apps connector, including those without first-class wrappers.",
  "type": "module",
  "main": "dist/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@azure/connectors": "0.2.0-preview",
    "@azure/functions": "^4.16.0",
    "@azure/functions-extensions-connectors": "0.0.2-preview"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "typescript": "^5.0.0"
  }
}
